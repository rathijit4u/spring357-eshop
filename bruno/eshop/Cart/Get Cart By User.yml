info:
  name: Get Cart By User
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/carts/users/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
