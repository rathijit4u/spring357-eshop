info:
  name: Get All Carts
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/carts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
