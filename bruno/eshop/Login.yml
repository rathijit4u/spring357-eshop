info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-TEST-ID
      value: eshop_login_test_1
  body:
    type: json
    data: |2-
        {
          "username": "bbanner_sci",
          "password": "GammaRay@2026"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
