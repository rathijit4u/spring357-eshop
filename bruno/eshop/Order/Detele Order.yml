info:
  name: Detele Order
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/orders/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
