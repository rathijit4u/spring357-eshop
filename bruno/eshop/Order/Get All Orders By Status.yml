info:
  name: Get All Orders By Status
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/orders/status/PENDING"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
