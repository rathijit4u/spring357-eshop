info:
  name: Update Order Status
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/api/orders/1/status"
  body:
    type: json
    data: |-
      {
        "status": "CONFIRMED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
