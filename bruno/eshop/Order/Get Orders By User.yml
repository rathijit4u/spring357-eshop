info:
  name: Get Orders By User
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/orders/user/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
