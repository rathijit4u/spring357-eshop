info:
  name: Get In-stock Products
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/products/in-stock"
  headers:
    - name: X-TEST-ID
      value: eshop_getInStockProduct_test_1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
