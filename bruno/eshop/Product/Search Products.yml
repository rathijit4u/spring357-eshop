info:
  name: Search Products
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/products/search?keyword=chair"
  headers:
    - name: X-TEST-ID
      value: eshop_searchProduct_test_1
  params:
    - name: keyword
      value: chair
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
