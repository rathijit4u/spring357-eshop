info:
  name: Add Product
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/products"
  body:
    type: json
    data: |-
      {
        "name": "Ceramic Coffee Mug",
        "description": null,
        "price": 499.00,
        "stockQuantity": 350,
        "category": "Kitchen",
        "sku": "HOME-MUG-012"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
