info:
  name: Delete Product
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/products/15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
