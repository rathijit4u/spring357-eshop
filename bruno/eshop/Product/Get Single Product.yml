info:
  name: Get Single Product
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/products/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
