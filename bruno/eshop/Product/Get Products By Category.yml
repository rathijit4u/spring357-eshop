info:
  name: Get Products By Category
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/products/category/Electronics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
