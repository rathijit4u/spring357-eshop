info:
  name: Search Products - Missing Keyword
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/api/products/search"
  headers:
    - name: X-TEST-ID
      value: eshop_searchProduct_test_1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
