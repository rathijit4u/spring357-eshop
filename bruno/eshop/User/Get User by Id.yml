info:
  name: Get User by Id
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/users/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
