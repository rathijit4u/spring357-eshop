info:
  name: Get All Users
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
